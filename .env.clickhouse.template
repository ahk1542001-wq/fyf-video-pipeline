# Copy to .env.clickhouse and fill real values. Never commit the copy.
# Used by scripts/deploy_cloudrun.sh (Secret Manager upload) and by the
# Data Officer MCP server + telemetry dual-write at runtime.
CLICKHOUSE_HOST=your-instance.asia-southeast1.gcp.clickhouse.cloud
CLICKHOUSE_PORT=8443
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=replace-with-a-strong-password
CLICKHOUSE_DATABASE=default
CLICKHOUSE_SECURE=true
